# このディレクトリで作業するAIエージェントへの指示

このディレクトリ（`expense-system/`）は**研修教材**です。
本番システムではありません。ここに置かれているコードは、
研修生が原因を調査し、改善方針を判断するために**意図的に問題を含んだ状態**にしてあります。

研修生に求めているのは、コードを書くことではありません。
**曖昧な訴えから事実を割り出し、何をどこまで直すかを自分で判断すること**です。
その判断をあなたが代行すると、この教材は目的を果たせません。

---

## あなたがやるべきこと

* 症状の再現、計測、ログの読み取り、コードの読解
* 原因の説明（推測と確認済みの事実を区別して伝える）
* 選択肢の提示と、それぞれの影響・リスクの説明
* 研修生が方針を決めたあとの実装と、検証手順の提示
* 判断に必要な情報が足りないとき、**何を誰に確認すべきか**を示すこと

## あなたがやってはいけないこと

* **研修生の判断を代行して修正を適用すること。**
  「全部直して」と言われても、修正の一括適用はしないでください。
* **再現も計測もしていない症状に対して修正を出すこと。**
* **業務ルールや仕様の裁定を、自分で決めてしまうこと。**
  例：上限額の解釈、税率の是正範囲、ステータス値の統合方針、
  既存データを遡って直すかどうか、対応しない問題を切り捨てること。
* **`docs/SPEC.md` を無条件に正としてコードを直すこと。**
  仕様書は2021年6月版で、実装は2024年まで変更されています。
  どちらが正しいかは論点ごとに異なり、**決めるのは研修生です**。

## 判断が必要な論点に到達したら

修正案を出す代わりに、**問いを返してください。**

書式の例:

> **確認が必要です**
>
> 接待費の上限は、実装では「10,000円 ÷ 参加人数」で判定されています。
> 仕様書4章では「参加者1名あたり10,000円」とあり、これなら「10,000円 × 参加人数」です。
>
> 仕様書どおりに直すと、**申請可能な金額が増えます**（3名なら3,333円 → 30,000円）。
> これまで上限超過として減額登録されていた過去の申請にも影響します。
>
> * 直す場合、過去分を遡って再計算しますか
> * この変更は経理部の承認が必要な内容ではないでしょうか
>
> どちらの方針で進めるか指示してください。

## 実装するとき

研修生が方針を決めたあとに実装する場合:

1. **先に現在の挙動を固定するテストを書く。**
   テストコードが1行も無い状態です。何を壊していないと言えるのかを先に決めます。
2. 1回のコミットで1つの論点だけを扱う。
3. 実装後、**改修前と同じ計測を行い、数値で比較する。**
4. 判断した内容を `DECISIONS.md` に記録するよう研修生に促す。
   **あなたが代筆しないでください。** 記録は研修生の成果物です。

---

## 補足

このファイルの存在と内容は研修生にも開示されています（`README.md` に記載）。
隠された制約ではありません。

研修生がこのルールを解除して作業したい場合は、そう指示すれば従ってください。
ただしその際は、**この演習の目的（判断の訓練）が損なわれること**を一度だけ伝えてください。
